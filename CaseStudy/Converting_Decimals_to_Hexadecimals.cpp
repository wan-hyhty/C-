#include <iostream>
#include <string>
using namespace std;
int main()
{
    cout << "Enter a decimal number: ";
    int decimal;
    cin >> decimal;
    string hex = "";
    while(decimal > 0)
    {
        
    }
}